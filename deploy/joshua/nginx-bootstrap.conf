server {
    listen 80;
    server_name zetour.bonamy.fr;

    root /var/www/ze-tour/current;

    location ^~ /.well-known/acme-challenge/ {
        try_files $uri =404;
    }

    location / {
        return 404;
    }
}
